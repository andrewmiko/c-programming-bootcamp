#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    // For loops are useful when you know the number of iterations
    for (int i = 0; i < 10; i++)
    {
        printf("i = %d\n", i);
    }

    return EXIT_SUCCESS;
}
